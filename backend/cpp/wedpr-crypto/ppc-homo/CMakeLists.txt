add_subdirectory(codec)
add_subdirectory(paillier)
add_subdirectory(fahe)
add_subdirectory(ihc)
if(DEMO)
    add_subdirectory(demo)
endif()
if (TESTS)
    enable_testing()
    set(CTEST_OUTPUT_ON_FAILURE TRUE)
    add_subdirectory(tests)
endif()
